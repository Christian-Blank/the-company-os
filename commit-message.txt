feat(rules-service): define base document models

Creates the base Pydantic models for the Company OS in `shared/libraries/company_os_core/models.py`.

This includes the `BaseDocument` model, which provides a common structure for all markdown-based documents in the system.

This completes Task 1.5 of Milestone 1.